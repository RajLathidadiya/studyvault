"use client";
import { FormEvent, useState } from "react";
export default function AdminLogin(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const data=await r.json();if(!r.ok)throw new Error(data.error||"Invalid credentials.");const me=await fetch("/api/auth/me",{cache:"no-store"}).then(x=>x.json());if(me.profile?.role!=="admin"){await fetch("/api/auth/logout",{method:"POST"});throw new Error("This account does not have administrator access.");}window.location.href="/admin";}catch(err){setError(err instanceof Error?err.message:"Unable to sign in.");}finally{setBusy(false);}}return <main className="admin-login-page"><section className="admin-login-card"><div className="admin-login-brand"><div className="admin-logo">SV</div><div><strong>StudyVault</strong><span>ADMIN CONSOLE</span></div></div><h1>Admin sign in</h1><p>Authorized administrators only.</p>{error&&<div className="admin-error">{error}</div>}<form onSubmit={submit}><label>Admin email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="admin@yourdomain.com" autoComplete="username" required/><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Your admin password" autoComplete="current-password" required/><button className="btn primary full" disabled={busy}>{busy?"Signing in…":"Sign in to Admin Console"}</button></form><p className="admin-login-foot"><a href="/">← Back to StudyVault</a></p></section></main>}
