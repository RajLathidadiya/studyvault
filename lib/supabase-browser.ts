// Supabase integration is intentionally disabled in this local build.
// The app uses the local demo auth/content layer until Supabase dependencies
// and environment variables are installed/configured.
export function createSupabaseBrowser() { return null; }
